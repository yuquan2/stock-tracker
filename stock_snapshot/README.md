# 股票池快照

本目录保存筛选器使用的股票列表快照。每个 `YYYYMMDD.csv` 文件记录对应日期的股票代码与名称，并从同日的 `data/YYYYMMDD.csv` 提取。

保留快照可固定历史数据下载和筛选时使用的股票池，避免后续上市、退市或股票名称变化影响已生成的数据。当前的 `20260902.csv` 是从当日行情快照提取的 4,404 只符合现行股票池规则的股票。

后续更新股票池时，新增一个以更新日期命名的 CSV 文件；已有快照不修改。
